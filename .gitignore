# Local secrets / runtime state — contains your API key, do NOT commit
config.json

# Python
__pycache__/
*.pyc

# Editor / tooling
.claude/

# Debug artifacts from the Debug button (may reflect screen contents)
debug_capture.png
debug_mask.png
